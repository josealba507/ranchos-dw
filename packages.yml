packages:
  # Macros genéricas de testing (unique_combination_of_columns, equal_rowcount,
  # etc.) — base para las pruebas de Uniqueness/Consistency de DAMA.
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]

  # Tests declarativos de calidad de datos (not_null_proportion, expect_column
  # values_to_be_between, etc.) — cubren las dimensiones de Completeness,
  # Validity y Accuracy de DAMA sin escribir SQL de test a mano.
  # (calogica/dbt_expectations quedó deprecado en favor de este fork bajo
  # metaplane — mismo namespace de macros, drop-in replacement.)
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]

  # Detección automática de anomalías (Fase 5 del documento de
  # especificación: "detección automática de anomalías sobre volumen de
  # filas, frescura y distribución de columnas" — complementa la
  # reconciliación exacta y el source freshness ya implementados, ver
  # docs/fase5_reconciliacion_raw.md). Compara contra el patrón histórico
  # real acumulado en corridas sucesivas de dbt, no contra umbrales fijos
  # adivinados. Ver docs/fase5_anomalias_elementary.md.
  - package: elementary-data/elementary
    version: [">=0.25.0", "<0.26.0"]
